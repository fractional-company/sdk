import Base from './base';
import { Connection, Options } from '../types/types';

export default class Nounlets extends Base {
  constructor(connection: Connection, options: Options) {
    super(connection, options);
  }
}
